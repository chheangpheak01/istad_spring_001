server:
  port: 9090
spring:
  application:
  name: restful-api-001
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: chph9771
    driver-class-name: org.postgresql.Driver
  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update